# Guardrail MVP 세부 기능 정의서 & 화면 정의서

**작성: Agent C (PM 디렉터) | 검수: Agent D (개발 디렉터)**
**작성일: 2026-02-22 | 최종 수정: 2026-02-23 | v1.4**

---

## 1. MVP 범위 요약

### 빌드 Phase 구분

**Phase 1 — 핵심 루프** (먼저 완성 → PM 디렉터 검수)
| # | 기능 | 설명 | 예상 공수 |
|---|------|------|----------|
| F1 | 회원가입/로그인 | 이메일+비밀번호, Google OAuth, GitHub OAuth | 1h |
| F2 | 프로젝트 관리 | 생성/삭제, 무료 최대 3개 | 1h |
| F3 | Project Key 발급 | 프로젝트별 고유 API 키 생성/재발급 | 0.5h |
| F5 | 보안 스캔 | 프로젝트에 연결된 스캔. 히스토리 저장 (로그인 필수) | 2h |
| F6 | 스캔 결과 화면 | 보안 등급 A~F + 문제 카드 (쉬운 말) | 1.5h |
| F8 | 대시보드 | 프로젝트 목록, 보안 등급, 최근 스캔 | 1.5h |

**Phase 2 — 백엔드 + 실제 동작 + 연결 & 모니터링** (Phase 1 검수 통과 후)
| # | 기능 | 설명 | 예상 공수 |
|---|------|------|----------|
| - | 인프라 세팅 | Supabase (DB + Auth + RLS) + 환경변수 + Rate Limit | 1h |
| F1~F6,F8 | 백엔드 실제 동작 | Auth/CRUD/Key/스캔엔진 → Mock 데이터 전면 교체 | 8h |
| F7 | MCP 서버 | Claude Code 등에서 연결 → 스캔 실행 (7 Tools, npm 패키지) | 2h |
| F9 | 실시간 모니터링 (Lite) | URL 기반 외부 모니터링 — SDK 불필요 (Vercel Cron) | 2h |
| F10 | SDK 런타임 모니터링 | 앱 내부 이벤트 감지 — 바이브코더는 "설치해줘" 한마디로 해결 | 3h |
| - | Stripe 결제 | Checkout/Portal/Webhook + 14일 무료 체험 | 1.5h |
| - | 배포 + 법적 | Vercel 배포 + 이용약관 + 개인정보처리방침 | 1.5h |

**Phase 3 — 이메일 알림** (Phase 2 완료 후)
| # | 기능 | 설명 | 예상 공수 |
|---|------|------|----------|
| F11 | 이메일 알림 | Resend 연동 — Runtime Event 알림 (brute force, traffic spike, admin access) + 모니터링 알림 (사이트 다운, SSL 만료, 보안 헤더) + 프로젝트별 on/off + 1시간 쿨다운 | 2h |

### F9 실시간 모니터링 상세 (URL 기반 — SDK 불필요)
| # | 항목 | 방법 | 사용자에게 보이는 말 |
|---|------|------|------------------|
| M1 | Uptime 체크 | HTTP ping 5분 간격 | "앱이 다운됐어요!" / "잘 돌아가고 있어요" |
| M2 | SSL 인증서 만료 | TLS 인증서 만료일 체크 | "SSL 인증서가 7일 후 만료돼요" |
| M3 | 보안 헤더 체크 | HTTP 응답 헤더 분석 | "보안 설정이 빠져있어요" |
| M4 | 자동 재스캔 | 매일 1회 GitHub repo 재스캔 | "어제 대비 새 문제가 생겼어요" |

### F10 SDK 런타임 모니터링 상세 (SDK 필요 — 바이브코더 마찰 없음)

**왜 MVP에 포함하는가:**
바이브코더는 Claude Code를 쓴다. "Guardrail SDK 설치해줘" 한마디면 끝.
SDK 설치는 이 타겟에게 마찰이 아니다. 개발 공수만 확보되면 MVP에 넣는 것이 맞다.

**SDK 연동 방식:**
```typescript
// 사용자 앱에 이 한 줄 추가 (Claude Code가 해줌)
import { guardrail } from 'guardrail-sdk'
app.use(guardrail({ projectKey: 'gr_sk_xxxx' }))
```

**감지 항목:**
| # | 감지 유형 | 알림 기준 | 사용자에게 보이는 말 |
|---|---------|----------|------------------|
| R1 | 브루트포스 | 10분 내 같은 IP 로그인 실패 10회+ | "누군가 비밀번호를 반복해서 시도하고 있어요" |
| R2 | 야간 관리자 접근 | 새벽 1~5시 관리자 계정 로그인 | "새벽 3시에 관리자 접근이 있었어요. 본인인가요?" |
| R3 | 트래픽 급증 | 평소 대비 요청량 500% 초과 | "갑자기 요청이 크게 늘었어요" |
| R4 | 처음 보는 위치 | 기존에 없던 국가 IP 첫 접근 | "처음 보는 나라에서 로그인했어요" |

**SDK 설치 안내 타이밍 정책:**

SDK 설치 안내는 사용자가 보안에 가장 관심이 높은 순간, 즉 **스캔 결과를 확인한 직후**에 노출한다.

| 채널 | 타이밍 | 안내 방식 |
|------|--------|----------|
| **웹 대시보드** | 스캔 결과 페이지 하단 | SDK setup prompt 카드 (SDK 미연결 시에만 표시) |
| **Claude Code (MCP)** | `guardrail_scan` 결과 텍스트 끝 | "Want real-time protection? Run guardrail_setup_sdk" 안내 문구 |
| **대시보드 Overview** | 항상 (SDK 미연결 시) | SDK setup prompt 카드 (기존 `sdk-setup-prompt.tsx`) |
| **Project Settings** | 항상 | SDK Monitoring 섹션 (기존 `setup-sdk-dialog.tsx`) |

원칙:
- SDK 미연결(`projects.sdk_connected = false`) 상태에서만 안내 표시
- 최초 스캔 완료 직후가 전환율이 가장 높은 타이밍 (보안 인식 피크)
- 안내는 비침투적(non-intrusive) — 카드/텍스트 형태, 모달 팝업 금지
- Claude Code에서는 "Guardrail SDK 설치해줘" 한마디로 설치 완료

### Should Have (이후 별도 스프린트)
| # | 기능 |
|---|------|
| S1 | 스캔 히스토리 비교 (이전 vs 현재) |
| S2 | MCP 자동 수정 가이드 (코드 수정 제안) |
| S3 | GitHub 커밋 실시간 유출 감지 |
| S4 | 알림 고도화 (Slack 연동, 알림 커스터마이징) |
| S5 | Slack 알림 연동 |
| S6 | 카드 테스트 감지 / 데이터 대량 수집 감지 |

### Won't Have (MVP에서 제외)
- 자동 코드 수정 (리스크 높음)
- CI/CD 연동
- 팀 플랜/결제/팀 초대
- 보안 등급 인증서/배지

---

## 2. 화면 정의

### Screen 1: Landing Page (`/`)

**목적**: 제품 소개 + 데모 스캔으로 가치 입증 + 회원가입 유도 (PLG 전환 퍼널).

```
┌──────────────────────────────────────────────────┐
│  🛡 Guardrail  Features Pricing  Sign In  [Sign Up Free] │
├──────────────────────────────────────────────────┤
│                                                  │
│         Ship your app                            │
│         without security fear.                   │
│                                                  │
│   ┌──────────────────────────┐ ┌──────────┐      │
│   │ https://github.com/repo  │ │ Scan Now │      │
│   └──────────────────────────┘ └──────────┘      │
│     Free for up to 3 projects                    │
│                                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│  │ Pre-     │ │ Real-    │ │ MCP      │         │
│  │ launch   │ │ time     │ │ Integra- │         │
│  │ Scan     │ │ Guard    │ │ tion     │         │
│  └──────────┘ └──────────┘ └──────────┘         │
└──────────────────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| Scan Now (Hero) | URL 입력 → 데모 스캔 로딩 → 잠금된 결과 모달 → Sign Up CTA |
| Sign In (텍스트) | → Login 화면 |
| Sign Up Free (primary 버튼) | → Sign Up 화면 |
| Features | 같은 페이지 내 Features 섹션으로 스크롤 |
| Pricing | 같은 페이지 내 Pricing 섹션으로 스크롤 |

**Navbar CTA 구조**:
- Sign In: 텍스트 링크 (보조 액션 — 기존 유저용)
- Sign Up Free: Primary 버튼 (`bg-gr-btn`) — 신규 전환 목적
- 모바일: 햄버거 메뉴에 Sign In 링크 + 상단 Sign Up Free 버튼 유지

**PLG 데모 스캔 플로우** (로그인 불필요):

```
URL 입력 → "Scan Now" 클릭
  → ScanLoadingState (8개 체크 항목 순차 애니메이션, ~3.4초)
  → DemoResultModal (잠금된 결과 + Signup CTA)
  → localStorage에 URL 저장
  → "Sign Up — It's Free →" 클릭 → /signup
```

| 단계 | 컴포넌트 | 동작 |
|------|---------|------|
| 1. 로딩 | `ScanLoadingState` | 8개 항목 380ms 간격 순차 체크 (Looking for exposed API keys, Checking payment security 등) |
| 2. 결과 | `DemoResultModal` | Preview 배너 + 등급 C + Issues 잠금 (lock 아이콘) + Passed 2개만 표시 + CTA |
| 3. 저장 | localStorage | `gr_scanned_url` 키로 URL 저장 → Dashboard Welcome 모달에서 pre-fill |

**스캔 접근 규칙**:
- 랜딩 데모 스캔: 로그인 불필요. 프론트엔드 목 데이터 기반 잠금된 미리보기 제공
- 전체 스캔/결과/대시보드: 로그인 필수
- 데모 결과에서는 이슈 제목+심각도만 표시, "How to fix?" 잠금 처리

#### Landing Page — Features 섹션 (`/#features`)

**목적**: Guardrail이 뭘 해주는지 3가지로 명확하게. 기술 용어 없이.

```
┌─────────────────────────────────────────────┐
│                                             │
│        Why Guardrail?                       │
│                                             │
│  ┌──────────────┐  ┌──────────────┐         │
│  │ 🔍           │  │ 🛡           │         │
│  │ Pre-launch   │  │ Real-time    │         │
│  │ Scan         │  │ Guard        │         │
│  │              │  │              │         │
│  │ Checks API   │  │ Monitors your│         │
│  │ keys, pass-  │  │ live app 24/7│         │
│  │ words, pay-  │  │ Uptime, SSL, │         │
│  │ ment. Plain  │  │ brute force  │         │
│  │ language.    │  │ & more.      │         │
│  └──────────────┘  └──────────────┘         │
│                                             │
│  ┌──────────────┐  ┌──────────────┐         │
│  │ ⚡           │  │ 📊           │         │
│  │ MCP          │  │ Security     │         │
│  │ Integration  │  │ Dashboard    │         │
│  │              │  │              │         │
│  │ Connect to   │  │ Security     │         │
│  │ Claude Code. │  │ grade at a   │         │
│  │ Scan without │  │ glance.      │         │
│  │ leaving your │  │ Track your   │         │
│  │ editor.      │  │ progress.    │         │
│  └──────────────┘  └──────────────┘         │
│                                             │
│  "How it works" — 3 steps                   │
│  ① Paste your GitHub URL (or scan from editor)│
│  ② Get your security grade in 30 seconds    │
│  ③ Fix issues with plain-language guides    │
│                                             │
└─────────────────────────────────────────────┘
```

| 요소 | 내용 |
|------|------|
| Feature 카드 4개 | Pre-launch Scan, Real-time Guard, MCP Integration, Security Dashboard |
| How it works | 3단계 설명 — 간결하게. 이미지/아이콘 동반 |
| 기술 용어 | 없음. 모든 설명이 일상 언어 |

**Feature 카드 상세**:
| # | 제목 | 설명 (사용자에게 보이는 그대로) |
|---|------|------|
| 1 | Pre-launch Scan | 노출된 API 키, 약한 비밀번호, 암호화 누락, 결제 보안 위험을 사용자보다 먼저 잡아줘요. |
| 2 | Real-time Guard | 업타임, SSL 인증서, 무차별 대입 시도, 의심 활동을 24시간 모니터링해요. |
| 3 | MCP Integration | Claude Code에 연결해서 에디터를 떠나지 않고 바로 스캔. 한 번의 명령, 즉시 결과. |
| 4 | Security Dashboard | 보안 등급을 한눈에. 진행 상황 추적, 단계별 가이드로 문제 해결. |

#### Landing Page — Pricing 섹션 (`/#pricing`)

**목적**: 가격이 비싸지 않다는 안도감. 무료도 모든 기능을 다 쓸 수 있다는 확신.
**핵심 원칙**: 무료와 유료의 유일한 차이 = 프로젝트 개수. 기능 차이 없음.

```
┌─────────────────────────────────────────────┐
│                                             │
│     Simple pricing. Start free.             │
│     Every feature. No limits.               │
│                                             │
│  ┌──────────────┐  ┌──────────────┐         │
│  │   Free       │  │  Pro         │         │
│  │   $0/month   │  │  $19/month   │         │
│  │              │  │              │         │
│  │  처음 써보는  │  │  프로젝트가  │         │
│  │  바이브코더   │  │  3개 이상    │         │
│  │  에게 딱!    │  │  필요할 때!  │         │
│  │              │  │              │         │
│  │  • 3 projects│  │  Everything  │         │
│  │  • All feat- │  │  in Free +   │         │
│  │    ures inc- │  │              │         │
│  │    luded     │  │  • Unlimited │         │
│  │  • Claude    │  │    projects  │         │
│  │    Code      │  │              │         │
│  │  • Live app  │  │              │         │
│  │    protect.  │  │              │         │
│  │  • Dashboard │  │              │         │
│  │              │  │              │         │
│  │  (CTA 없음)  │  │ [Upgrade to │         │
│  │              │  │   Pro]       │         │
│  │              │  │              │         │
│  └──────────────┘  └──────────────┘         │
│                                             │
│  🎉 연간 결제 시 2개월 무료!               │
│                                             │
└─────────────────────────────────────────────┘
```

**요금제 상세**:
| 플랜 | 가격 | 대상 | 포함 기능 |
|------|------|------|----------|
| Free | $0/월 | 처음 써보는 바이브코더 | 프로젝트 3개, 월 10회 스캔, 최근 3개 히스토리, 보안 등급, Claude Code 연동, 대시보드, Community support |
| Pro | $19/월 | 본격적으로 쓰는 바이브코더 | 프로젝트 무제한, 무제한 스캔, 전체 히스토리, 라이브 앱 보호, 24/7 모니터링, Priority support |

**Pricing UX 원칙**:
- Free와 Pro의 차이 = 프로젝트 개수 + 스캔 횟수(월 10회 vs 무제한) + 히스토리(최근 3개 vs 전체) + 지원 수준
- Free 플랜에는 CTA 없음 (현재 사용 중)
- Landing Page: Pro → "Upgrade to Pro" CTA (검은색 버튼)
- Plan & Billing 페이지: Pro → "Start Free Trial" CTA + "14-day free trial"
- 두 박스 모두 동일한 디자인 (흰색 배경, 검은 텍스트)
- 연간 할인 배너: "Save 2 months with annual billing!"
- "모든 기능이 무료"를 강조 → 전환 허들 최소화

#### Landing Page — Footer

```
┌─────────────────────────────────────────────┐
│  🛡 Guardrail                               │
│  Security for Vibe Coders                   │
│                                             │
│  Product     Legal          Connect         │
│  Features    Privacy        X (Twitter)     │
│  Pricing     Terms          GitHub          │
│  Docs                                       │
│                                             │
│  © 2026 Guardrail. All rights reserved.     │
└─────────────────────────────────────────────┘
```

---

### 공통 사이드바 구조

모든 로그인 화면에서 동일한 사이드바를 사용:

```
┌──────────────────┐
│ 🛡 Guardrail     │
│                  │
│ ┌──────────────┐ │  ← 프로젝트 드롭다운
│ │ A My SaaS App│ │    (클릭 시 프로젝트 전환)
│ │   saas.app  ⌄│ │
│ └──────────────┘ │
│                  │
│ 📊 Overview      │  ← 등급 + 모니터링 + SDK이벤트 + 최근스캔
│ 🔍 Scan          │  ← 보안 스캔 실행 + 결과
│ 📋 Scan History  │  ← 스캔 히스토리 전체 목록
│ ⚙️ Settings      │  ← 프로젝트 설정
│                  │
│ ┌──────────────┐ │
│ │ Free Plan    │ │
│ │ 2/3 used ■■□ │ │
│ │ [Upgrade]    │ │
│ └──────────────┘ │
│                  │
│ 🟤 Park          │  ← 유저 프로필
│   park@email.com │    (클릭 → Account Settings)
└──────────────────┘
```

| 요소 | 동작 |
|------|------|
| 프로젝트 드롭다운 | 클릭 → 프로젝트 목록 + "New Project" 옵션 |
| Overview | 선택된 프로젝트의 전체 현황판 (등급, Uptime, SSL, Issues, SDK Events, Recent Scans) |
| Scan | 보안 스캔 실행 + 결과 확인 |
| Scan History | 선택된 프로젝트의 스캔 히스토리 전체 목록 |
| Settings | 프로젝트 설정 (General, MCP Connection, Danger Zone) |
| Plan Info | 현재 플랜 + 사용량 + Upgrade 버튼 |
| User Profile | 클릭 → Account Settings (프로필, 플랜, 로그아웃) |

---

### Screen 2: Dashboard / Overview (`/dashboard`)

**목적**: 선택된 프로젝트의 요약 대시보드.

```
┌────────┬────────────────────────────────────┐
│ 🛡     │  Overview             [Run Scan]   │
│        │  My SaaS App • Last scan: 2hrs ago │
│ [A ▾]  ├────────────────────────────────────┤
│ My SaaS│                                    │
│        │ ┌────────┐┌────────┐┌────────┐┌──┐ │
│ Overvw │ │Security││ Uptime ││  SSL   ││Is│ │
│ ■ ──── │ │Grade A ││ 100%   ││287 day ││0 │ │
│ Scans  │ │All clr ││ Online ││ Valid  ││  │ │
│ Monit  │ └────────┘└────────┘└────────┘└──┘ │
│ Settin │                                    │
│        │ Recent Scans          View all →   │
│ ────── │ ┌────────────────────────────────┐ │
│Free    │ │ A Today 10:23  MCP  8/8 clear  │ │
│2/3 used│ │ B Yesterday   Web  7/8 1 warn  │ │
│[Upgrad]│ │ C Feb 20      Auto 5/8 3 issue │ │
│        │ └────────────────────────────────┘ │
│ P Park │                                    │
└────────┴────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| Stats 카드 4개 | Security Grade, Uptime, SSL, Open Issues |
| Security Grade Guide | A~F 등급별 의미 설명 카드 (아래 참조) |
| Recent Events | SDK 런타임 이벤트 (브루트포스, 이상 로그인 등) — SDK 연결 시 표시 |
| Recent Scans | 최근 스캔 3건 표시 + "View all →" → Scan History |
| Run Scan | 선택된 프로젝트 재스캔 |

**Security Grade Guide (Overview 하단)**:
| 등급 | 라벨 | 색상 | 설명 |
|------|------|------|------|
| A | Excellent | Green | Your app passed all security checks. Safe to deploy. |
| B | Good | Green | Minor improvements recommended but generally secure. |
| C | Fair | Amber | Several issues found. Review before deploying. |
| D | Poor | Orange | Significant security risks detected. Fix before going live. |
| F | Critical | Red | Critical vulnerabilities found. Immediate action required. |

**Stats 카드 정보**:
- Security Grade (A~F) + 색상 배지 + 상태 메시지
- Uptime (100%) + Online/Down 상태 — URL Monitoring (F9)
- SSL Certificate (남은 일수) + Valid/Expiring 상태 — URL Monitoring (F9)
- Open Issues (개수) + No issues / N issues

> Overview가 프로젝트의 "전체 현황판" 역할. 기존 Monitoring 탭을 Overview에 통합.

---

### Screen 3: Scan Result (`/project/[id]/scan/[scanId]`)

**목적**: 핵심 가치 화면. 보안 등급과 문제를 쉬운 말로 보여줌.

```
┌────────┬────────────────────────────────────┐
│ 🛡     │  My SaaS App          [Rescan]     │
│        ├────────────────────────────────────┤
│ Projs  │                                    │
│ ■ ──── │  ┌────────────────────────────┐    │
│ History│  │    보안 등급                │    │
│ Alerts │  │       A                    │    │
│ ───    │  │  ✅ 배포해도 안전해요!     │    │
│ Settin │  │  Scanned 2 min ago         │    │
│        │  └────────────────────────────┘    │
│        │                                    │
│        │  통과한 항목 (8)                    │
│        │  ┌────────────────────────────┐    │
│        │  │ ✅ Stripe 키 안전하게 보관 │    │
│        │  │ ✅ 비밀번호 암호화 확인    │    │
│        │  │ ✅ API 키 노출 없음        │    │
│        │  │ ...                        │    │
│        │  └────────────────────────────┘    │
│        │                                    │
│        │  고쳐야 할 것 (2)                   │
│        │  ┌────────────────────────────┐    │
│        │  │ 🔴 Stripe 비밀 키 노출     │    │
│        │  │ 누구나 이 키로 결제를      │    │
│        │  │ 조작할 수 있어요           │    │
│        │  │        [어떻게 고치나요?]   │    │
│        │  └────────────────────────────┘    │
│        │  ┌────────────────────────────┐    │
│        │  │ 🟡 로그인 시도 제한 없음   │    │
│        │  │ 누군가 비밀번호를 계속     │    │
│        │  │ 시도할 수 있어요           │    │
│        │  │        [어떻게 고치나요?]   │    │
│        │  └────────────────────────────┘    │
└────────┴────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| 보안 등급 카드 | A~F 등급 + 색상 + 한줄 요약 |
| 통과 항목 리스트 | 접기/펼치기 가능. 기본 접힌 상태 |
| 문제 카드 | 아이콘(🔴/🟡) + 쉬운말 설명 + "How to fix?" 버튼 → 모달 |
| "How to fix?" | 클릭 → 모달 다이얼로그 → 단계별 수정 가이드 (코드 스니펫 포함) |
| Security Grade 가이드 | 하단에 A~F 등급 의미 설명 카드 (Overview와 동일) |
| Rescan 버튼 | 재스캔 실행 |

**보안 등급 기준**:
| 등급 | 조건 | 색상 | 메시지 |
|------|------|------|--------|
| A | 모든 항목 통과 | Green | 배포해도 안전해요! |
| B | 경미한 이슈 1~2개 | Green | 거의 완벽해요! 조금만 손보면 돼요 |
| C | 중간 이슈 1~3개 | Amber | 고칠 게 몇 가지 있어요 |
| D | 심각한 이슈 1~2개 | Orange | 배포 전에 꼭 고쳐야 해요 |
| F | 심각한 이슈 3개+ | Red | 지금 고쳐야 해요! |

**문제 카드 구조** (절대 보여주지 않는 것: CVE, CVSS, 에러코드):
```
[아이콘] [제목: 쉬운 말]
[설명: 왜 위험한지 한 문장]
[수정 가이드: 코드 스니펫 + 설명] (접기/펼치기)
```

---

### Screen 3-1: Scan History (`/project/[id]/scans`)

**목적**: 선택된 프로젝트의 전체 스캔 히스토리. Dashboard Overview의 "View all →" 링크에서 진입.

```
┌────────┬────────────────────────────────────┐
│ 🛡     │  Scan History                      │
│        │  My SaaS App                       │
│ [A ▾]  ├────────────────────────────────────┤
│ My SaaS│                                    │
│        │  ┌──────────────────────────────┐  │
│ Overvw │  │ Grade  Date         Source   │  │
│ Scan   │  │ Checks Status              │  │
│ ScnHis │  ├──────────────────────────────┤  │
│   ■    │  │  A    Today 10:23    MCP    │  │
│ Settin │  │  8/8  All checks passed     │  │
│        │  ├──────────────────────────────┤  │
│ ────── │  │  B    Yesterday      Web    │  │
│Free    │  │  7/8  1 warning             │  │
│2/3 used│  ├──────────────────────────────┤  │
│[Upgrad]│  │  C    Feb 20         Auto   │  │
│        │  │  5/8  3 issues              │  │
│ P Park │  ├──────────────────────────────┤  │
│        │  │  B    Feb 19         Web    │  │
│        │  │  7/8  1 warning             │  │
│        │  ├──────────────────────────────┤  │
│        │  │  D    Feb 18         MCP    │  │
│        │  │  4/8  4 issues              │  │
│        │  ├──────────────────────────────┤  │
│        │  │  F    Feb 17         Web    │  │
│        │  │  2/8  6 issues              │  │
│        │  └──────────────────────────────┘  │
└────────┴────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| 스캔 행 클릭 | → Scan Result (`/project/[id]/scan/[scanId]`) 화면으로 이동 |
| Grade 배지 | A~F 등급 + 색상 (A/B=Green, C=Amber, D=Orange, F=Red) |
| Source 태그 | Web / MCP / Auto — 스캔 출처 구분 |
| Checks 요약 | 통과/전체 + 상태 메시지 |
| 정렬 | 최신순 (created_at DESC) |
| 페이지네이션 | 무한 스크롤 또는 "Load more" 버튼 (스캔 20개 이상 시) |

---

### Screen 4: Project Settings (`/project/[id]/settings`)

**목적**: 프로젝트 설정 + MCP 연결 + 모니터링 설정.

```
┌────────┬────────────────────────────────────┐
│ 🛡     │  My SaaS App > Settings            │
│        ├────────────────────────────────────┤
│ Projs  │                                    │
│   ──── │  General                           │
│ History│  ┌────────────────────────────┐    │
│ Alerts │  │ Project Name               │    │
│ ───    │  │ [My SaaS App          ]    │    │
│ Settin │  │ Project URL                │    │
│   ■    │  │ [https://my-saas.app  ]    │    │
│        │  └────────────────────────────┘    │
│        │                                    │
│        │  MCP Connection                    │
│        │  ┌────────────────────────────┐    │
│        │  │ Project Key                │    │
│        │  │ [gr_sk_a1b2c3...  ] [Copy] │    │
│        │  │ [Regenerate Key]           │    │
│        │  │                            │    │
│        │  │ Claude Code 설정:          │    │
│        │  │ ┌────────────────────┐     │    │
│        │  │ │ { "guardrail": {  │     │    │
│        │  │ │   "command":"npx", │     │    │
│        │  │ │   ...             │     │    │
│        │  │ │ }}                │     │    │
│        │  │ └────────────────────┘     │    │
│        │  └────────────────────────────┘    │
│        │                                    │
│        │  Monitoring                        │
│        │  ┌────────────────────────────┐    │
│        │  │ URL Monitoring     [ON]    │    │
│        │  │ ✅ Uptime: Online          │    │
│        │  │ ✅ SSL: 287 days left      │    │
│        │  │ ⚠️ Security Headers: 2/6   │    │
│        │  │ Next scan: in 6 hours      │    │
│        │  ├────────────────────────────┤    │
│        │  │ SDK Monitoring    [Setup]  │    │
│        │  │ "Claude Code에서            │    │
│        │  │  'Guardrail SDK 설치해줘'  │    │
│        │  │  라고 말하세요"            │    │
│        │  └────────────────────────────┘    │
│        │                                    │
│        │  Danger Zone                       │
│        │  ┌────────────────────────────┐    │
│        │  │ [Delete Project]           │    │
│        │  └────────────────────────────┘    │
└────────┴────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| Project Name / URL | 수정 가능, 자동 저장 |
| Project Key | 마스킹 표시, Copy 버튼으로 복사 |
| Regenerate Key | 확인 다이얼로그 후 새 키 발급 (이전 키 무효화) |
| MCP 설정 가이드 | JSON 코드블록 (Copy 버튼 포함) |
| URL Monitoring 토글 | ON/OFF — URL 입력 시 자동 ON |
| SDK Monitoring Setup | 클릭 → SDK 설치 가이드 표시 |
| Delete Project | 확인 다이얼로그 후 삭제 |

**Project Key 형식**: `gr_sk_` + 32자 랜덤 문자열
**Key 보안**: 발급 시 1회만 전체 표시. 이후 마스킹 (`gr_sk_a1b2...xxxx`)

---

### Screen 5: Monitoring Dashboard — Overview에 통합 (별도 라우트 없음)

**목적**: 프로젝트별 모니터링 현황. ~~별도 `/project/[id]/monitoring` 라우트 없음~~ → **Overview (`/dashboard`)에 통합.**

> **PM Director 결정 (2026-02-23)**: Monitoring 데이터(Uptime, SSL, Headers, Runtime Events)는 Overview의 Stats 카드 4개 + Runtime Events 섹션에 통합. 별도 페이지는 정보 분산을 야기하고, 사용자가 Overview 한 화면에서 전체 현황을 파악하는 것이 UX적으로 우월. 사이드바 네비에 Monitoring 탭 추가 불필요. 아래 와이어프레임은 Overview 내 모니터링 영역의 레퍼런스로 보존.

```
┌────────┬────────────────────────────────────┐
│ 🛡     │  My SaaS App > Monitoring          │
│        ├────────────────────────────────────┤
│        │                                    │
│        │  오늘 앱은 안전해요 ✅              │
│        │                                    │
│        │  ┌──────┐ ┌──────┐ ┌──────┐       │
│        │  │Uptime│ │ SSL  │ │Headers│      │
│        │  │ 100% │ │287일 │ │ 4/6  │       │
│        │  └──────┘ └──────┘ └──────┘       │
│        │                                    │
│        │  최근 이벤트                        │
│        │  ┌────────────────────────────┐    │
│        │  │ 🔴 10:23 브루트포스 감지   │    │
│        │  │ 같은 IP에서 로그인 15회    │    │
│        │  │ 시도        [차단하기]      │    │
│        │  ├────────────────────────────┤    │
│        │  │ 🟡 08:15 새 위치 로그인    │    │
│        │  │ 독일에서 첫 접근           │    │
│        │  │            [본인이에요]     │    │
│        │  ├────────────────────────────┤    │
│        │  │ ✅ 07:00 일간 리포트       │    │
│        │  │ 어제 앱은 평화로웠어요     │    │
│        │  └────────────────────────────┘    │
└────────┴────────────────────────────────────┘
```

---

### Screen L1: Login (`/login`)

**목적**: 기존 사용자 로그인.

```
┌─────────────────────────────────────────────┐
│                                             │
│              🛡 Guardrail                   │
│                                             │
│         ┌─────────────────────┐             │
│         │                     │             │
│         │  Welcome back       │             │
│         │  Sign in to your    │             │
│         │  account            │             │
│         │                     │             │
│         │  Email              │             │
│         │  ┌───────────────┐  │             │
│         │  │               │  │             │
│         │  └───────────────┘  │             │
│         │  Password           │             │
│         │  ┌───────────────┐  │             │
│         │  │               │  │             │
│         │  └───────────────┘  │             │
│         │  Forgot password? → │             │
│         │                     │             │
│         │  [    Sign In     ] │             │
│         │                     │             │
│         │  ──── or ────       │             │
│         │                     │             │
│         │  [Continue w Google]│             │
│         │                     │             │
│         │  Don't have an      │             │
│         │  account? Sign up → │             │
│         └─────────────────────┘             │
│                                             │
└─────────────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| Sign In 버튼 | 이메일+비밀번호 로그인 → Dashboard |
| Continue with Google | Google OAuth → Dashboard |
| Continue with GitHub | GitHub OAuth → Dashboard |
| Forgot password? | 비밀번호 리셋 이메일 발송 |
| Sign up 링크 | → Sign Up 화면 |

---

### Screen L2: Sign Up (`/signup`)

**목적**: 신규 사용자 가입.

```
┌─────────────────────────────────────────────┐
│                                             │
│              🛡 Guardrail                   │
│                                             │
│         ┌─────────────────────┐             │
│         │                     │             │
│         │  Create your        │             │
│         │  account            │             │
│         │                     │             │
│         │  Name               │             │
│         │  ┌───────────────┐  │             │
│         │  │               │  │             │
│         │  └───────────────┘  │             │
│         │  Email              │             │
│         │  ┌───────────────┐  │             │
│         │  │               │  │             │
│         │  └───────────────┘  │             │
│         │  Password           │             │
│         │  ┌───────────────┐  │             │
│         │  │               │  │             │
│         │  └───────────────┘  │             │
│         │  Must be 8+ chars   │             │
│         │                     │             │
│         │  [  Create Account ]│             │
│         │                     │             │
│         │  ──── or ────       │             │
│         │                     │             │
│         │  [Continue w Google]│             │
│         │                     │             │
│         │  Already have an    │             │
│         │  account? Sign in → │             │
│         │                     │             │
│         │  Terms & Privacy    │             │
│         └─────────────────────┘             │
│                                             │
└─────────────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| Create Account 버튼 | 이메일+비밀번호 가입 → 이메일 인증 → Dashboard |
| Continue with Google | Google OAuth → Dashboard |
| Continue with GitHub | GitHub OAuth → Dashboard |
| Sign in 링크 | → Login 화면 |
| Terms / Privacy 링크 | → 약관/개인정보처리방침 페이지 |

---

### Screen L3: Email Verification Pending (`/auth/verify-email`)

**목적**: 이메일+비밀번호 가입 후 인증 대기 화면. OAuth 가입은 이 화면을 거치지 않음. (Phase 2 — 백엔드 연동 시 구현)

```
┌─────────────────────────────────────────────┐
│                                             │
│              🛡 Guardrail                   │
│                                             │
│         ┌─────────────────────┐             │
│         │                     │             │
│         │  📧 이메일을 확인   │             │
│         │  해주세요           │             │
│         │                     │             │
│         │  park@email.com으로  │             │
│         │  인증 메일을 보냈어  │             │
│         │  요. 링크를 클릭하면 │             │
│         │  바로 시작할 수 있어 │             │
│         │  요!                │             │
│         │                     │             │
│         │  메일이 안 왔나요?   │             │
│         │  [다시 보내기]       │             │
│         │                     │             │
│         │  다른 이메일로       │             │
│         │  가입하기 →          │             │
│         │                     │             │
│         └─────────────────────┘             │
│                                             │
└─────────────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| 인증 대기 메시지 | 가입한 이메일 주소 표시 |
| 다시 보내기 버튼 | 인증 메일 재발송 (`supabase.auth.resend()`) |
| 다른 이메일로 가입하기 | → Sign Up 화면으로 이동 |
| 인증 완료 시 | 자동으로 Dashboard로 리다이렉트 |

**진입 조건**: 이메일+비밀번호 가입 직후 자동 이동. 인증 완료 전 `/dashboard` 접근 시에도 이 화면으로 리다이렉트.

---

### Screen L4: Forgot Password (`/auth/forgot-password`)

**목적**: 비밀번호를 잊은 사용자가 재설정 이메일을 요청. (Phase 2 — 백엔드 연동 시 구현)

```
┌─────────────────────────────────────────────┐
│                                             │
│              🛡 Guardrail                   │
│                                             │
│         ┌─────────────────────┐             │
│         │                     │             │
│         │  비밀번호 재설정     │             │
│         │                     │             │
│         │  가입한 이메일을     │             │
│         │  입력하면 재설정     │             │
│         │  링크를 보내드려요.  │             │
│         │                     │             │
│         │  Email              │             │
│         │  ┌───────────────┐  │             │
│         │  │               │  │             │
│         │  └───────────────┘  │             │
│         │                     │             │
│         │  [  Send Reset Link ]│             │
│         │                     │             │
│         │  ← Back to Sign In  │             │
│         │                     │             │
│         └─────────────────────┘             │
│                                             │
└─────────────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| Email 입력 | 가입된 이메일 주소 입력 |
| Send Reset Link | `supabase.auth.resetPasswordForEmail()` → 성공 시 확인 메시지 |
| 성공 메시지 | "재설정 링크를 보냈어요. 이메일을 확인해주세요." (같은 화면에서 표시) |
| Back to Sign In | → Login 화면 |

**보안**: 존재하지 않는 이메일도 동일한 성공 메시지 표시 (이메일 존재 여부 노출 방지).

---

### Screen L5: Reset Password (`/auth/reset-password`)

**목적**: 재설정 링크를 통해 새 비밀번호 설정. (Phase 2 — 백엔드 연동 시 구현)

```
┌─────────────────────────────────────────────┐
│                                             │
│              🛡 Guardrail                   │
│                                             │
│         ┌─────────────────────┐             │
│         │                     │             │
│         │  새 비밀번호 설정    │             │
│         │                     │             │
│         │  New Password       │             │
│         │  ┌───────────────┐  │             │
│         │  │               │  │             │
│         │  └───────────────┘  │             │
│         │  Must be 8+ chars   │             │
│         │                     │             │
│         │  Confirm Password   │             │
│         │  ┌───────────────┐  │             │
│         │  │               │  │             │
│         │  └───────────────┘  │             │
│         │                     │             │
│         │  [ Reset Password ] │             │
│         │                     │             │
│         └─────────────────────┘             │
│                                             │
└─────────────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| New Password | 8자 이상 새 비밀번호 입력 |
| Confirm Password | 비밀번호 확인 입력 |
| Reset Password | `supabase.auth.updateUser({ password })` → 성공 시 Login으로 이동 |
| 성공 메시지 | "비밀번호가 변경됐어요. 로그인해주세요." → 자동으로 Login 화면 이동 |

**진입 조건**: Supabase 재설정 이메일의 링크 클릭 시. 직접 URL 접근 시 토큰 검증 실패 → "링크가 만료됐어요. 다시 요청해주세요." + Forgot Password 링크.

---

### Screen 6: Profile Settings (`/settings/profile`)

**목적**: 사용자 프로필 관리 + 외관 설정.

```
┌────────┬────────────────────────────────────┐
│ 🛡     │  Account > Profile                 │
│        ├────────────────────────────────────┤
│ [A ▾]  │                                    │
│ My SaaS│  Profile                           │
│        │  ┌────────────────────────────┐    │
│ Overvw │  │ Name                       │    │
│ Scan   │  │ [Park                 ]    │    │
│ Scans  │  │ Email                      │    │
│ Settin │  │ park@email.com (read only) │    │
│        │  └────────────────────────────┘    │
│ ────── │                                    │
│Free    │  Appearance                        │
│2/3 used│  ┌────────────────────────────┐    │
│[Upgrad]│  │  ┌─ Light ──┐ ┌─ Dark ──┐ │    │
│        │  │  │ [선택됨]  │ │         │ │    │
│ P Park │  │  │ 미니 프리 │ │ 미니 프리│ │    │
│        │  │  │ 뷰 (밝은 │ │ 뷰 (어두│ │    │
│        │  │  │ 테마)    │ │ 운 테마) │ │    │
│        │  │  └──────────┘ └─────────┘ │    │
│        │  └────────────────────────────┘    │
│        │                                    │
└────────┴────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| Name | 읽기 모드(텍스트) + Edit 버튼 → 편집 모드(입력) + Cancel/Save 버튼 |
| Email | 읽기 전용 (OAuth 제공) |
| Light / Dark 선택 | 테마 전환 (미니 프리뷰 카드로 시각화) |
| Change Password (Email) | 읽기 모드(•••• + Edit) → 편집 모드(3개 입력 + Cancel/Update Password) |
| Change Password (OAuth) | "You signed in with [Provider]." + 외부 Settings 링크 (GitHub/Google) |
| Delete Account | Danger Zone — 확인 후 계정 영구 삭제 |

**Change Password — OAuth 사용자 UI (GitHub 예시)**:
```
┌─────────────────────────────────────────┐
│  Change Password                        │
│                                         │
│  [GitHub 아이콘]                        │
│  You signed in with GitHub. Passwords   │
│  are managed by your GitHub account.    │
│                                         │
│  Manage in GitHub Settings →            │  ← 외부 링크
└─────────────────────────────────────────┘
```

**Change Password — Email/Password 사용자 UI (편집 모드)**:
```
┌─────────────────────────────────────────┐
│  Change Password                        │
│                                         │
│  Current Password                       │
│  ┌─────────────────────────────────────┐│
│  │ ••••••••                            ││
│  └─────────────────────────────────────┘│
│  New Password                           │
│  ┌─────────────────────────────────────┐│
│  │ ••••••••                            ││
│  └─────────────────────────────────────┘│
│  Confirm Password                       │
│  ┌─────────────────────────────────────┐│
│  │ ••••••••                            ││
│  └─────────────────────────────────────┘│
│                                         │
│  [Cancel]  [Update Password]            │
└─────────────────────────────────────────┘
```

**외부 링크 매핑**:
| Provider | Settings URL |
|---|---|
| GitHub | `https://github.com/settings/security` |
| Google | `https://myaccount.google.com/security` |

**Component Props**:
```tsx
interface ChangePasswordProps {
  authProvider?: "email" | "github" | "google";  // default: "email"
}
```
`authProvider` 값은 사용자 세션/프로필에서 가져온다. 현재 프론트엔드에서는 prop으로 전달받아 mock 가능.

---

### Screen 7: Plan & Billing (`/settings/billing`)

**목적**: 현재 플랜 확인 + Pro 업그레이드.

```
┌────────┬────────────────────────────────────┐
│ 🛡     │  Account > Plan & Billing          │
│        ├────────────────────────────────────┤
│        │                                    │
│        │  ┌──────────────┐  ┌──────────────┐│
│        │  │ Current Plan │  │  Pro Plan    ││
│        │  │ Free Plan    │  │  $19/month   ││
│        │  │              │  │  For growing ││
│        │  │ • 3 projects │  │  teams       ││
│        │  │ • All feat.  │  │              ││
│        │  │ • Community  │  │ • Unlimited  ││
│        │  │   support    │  │   projects   ││
│        │  │              │  │ • All feat.  ││
│        │  │  (CTA 없음)  │  │ • Priority   ││
│        │  │              │  │   support    ││
│        │  │              │  │              ││
│        │  │              │  │ [Start Free  ││
│        │  │              │  │   Trial]     ││
│        │  │              │  │ 14-day trial ││
│        │  └──────────────┘  └──────────────┘│
│        │                                    │
│        │  Billing Info                      │
│        │  ┌────────────────────────────┐    │
│        │  │ No billing info on file.   │    │
│        │  │ [Add Payment Method]       │    │
│        │  └────────────────────────────┘    │
│        │                                    │
└────────┴────────────────────────────────────┘
```

**Plan & Billing 상세**:
- Free 플랜: 3 projects, All features, Community support — CTA 없음 (현재 사용 중)
- Pro 플랜: Unlimited projects, All features, Priority support — "Start Free Trial" CTA + 14-day 무료 체험
- Billing Info: 결제 정보 미등록 시 "Add Payment Method" 버튼

---

## 3. 기능 상세

### F1: 회원가입/로그인

| 항목 | 내용 |
|------|------|
| 방법 | 이메일+비밀번호, Google OAuth, GitHub OAuth (3가지) |
| 수집 정보 | 이메일 (필수), 이름 (선택, 회원가입 시), 프로필 이미지 (OAuth 시 자동) |
| 이메일+비밀번호 | Supabase Auth 내장 — 이메일 인증 필수, 비밀번호 찾기 지원 |
| OAuth 로그인 | Google/GitHub 1클릭 가입. 이메일 인증 불필요. 자동 계정 생성 |
| 첫 로그인 시 | Dashboard로 이동 |

### F2: 프로젝트 관리

| 항목 | 내용 |
|------|------|
| 생성 시 입력 | 프로젝트 이름 (필수), URL (선택), GitHub Repository (OAuth 연동 또는 수동 URL) |
| GitHub 연동 | OAuth로 GitHub 계정 연동 시 private repo 포함 검색/선택 가능. 미연동 시 public repo URL 수동 입력 |
| 무료 한도 | 최대 3개 |
| 한도 초과 시 | "업그레이드하면 더 만들 수 있어요" 안내 |
| 삭제 | 확인 다이얼로그 필수. 스캔 히스토리도 함께 삭제 |

### F2-1: GitHub OAuth Integration 상세

New Project 다이얼로그에서 GitHub OAuth를 통해 private repository를 검색/선택할 수 있는 기능의 상세 스펙.

#### OAuth Configuration

| Scope | Purpose |
|-------|---------|
| `repo` | Private repository 접근 (코드 읽기, 스캔) |
| `read:user` | 사용자 프로필 정보 (username, avatar) |

```
Application name:  GuardRail
Homepage URL:      https://guardrail.dev
Callback URL:      https://guardrail.dev/api/auth/github/callback
                   (dev: http://localhost:3000/api/auth/github/callback)
```

#### GitHub 연동 User Flow

**Flow A: 계정 미연동 (최초)**
```
1. New Project 다이얼로그 → "Connect GitHub" 버튼 표시
2. 버튼 클릭 → GitHub OAuth 동의 화면 리다이렉트
3. 권한 승인 → 콜백 → "Connected as @username" 표시
4. Repository 검색 드롭다운 활성화 → repo 검색/선택 (private 포함)
```

**Flow B: 계정 이미 연동**
```
1. New Project 다이얼로그 → "Connected as @username" 바로 표시
2. Repository 검색 드롭다운에서 바로 repo 선택
```

**Flow C: 수동 URL 입력 (폴백)**
```
1. "Or enter URL manually" 클릭 → URL 입력 필드 표시
2. Public repo만 이 방식으로 연결 가능
```

#### New Project — GitHub 연동 UI States

**State 1: Not Connected (기본)**
```
┌─────────────────────────────────────────┐
│  GitHub Repository                      │
│                                         │
│  ┌─────────────────────────────────────┐│
│  │  [GitHub 아이콘]  Connect GitHub    ││  ← 버튼
│  └─────────────────────────────────────┘│
│                                         │
│  Or enter URL manually                  │  ← 텍스트 링크
│                                         │
│  We'll scan your code for security      │
│  issues                                 │
└─────────────────────────────────────────┘
```

**State 2: Connecting (로딩)**
```
┌─────────────────────────────────────────┐
│  GitHub Repository                      │
│                                         │
│  ┌─────────────────────────────────────┐│
│  │  [스피너]  Connecting to GitHub...  ││
│  └─────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

**State 3: Connected — Repo 미선택**
```
┌─────────────────────────────────────────┐
│  GitHub Repository                      │
│                                         │
│  ✓ Connected as @parkdev        [변경]  │
│                                         │
│  ┌─────────────────────────────────────┐│
│  │  🔍  Search repositories...        ││  ← 검색 드롭다운
│  └─────────────────────────────────────┘│
│                                         │
│  We'll scan your code for security      │
│  issues                                 │
└─────────────────────────────────────────┘
```

**State 4: Connected — Repo 선택 완료**
```
┌─────────────────────────────────────────┐
│  GitHub Repository                      │
│                                         │
│  ✓ Connected as @parkdev        [변경]  │
│                                         │
│  ┌─────────────────────────────────────┐│
│  │  🔒 my-org/private-app        [✕]  ││  ← 선택된 repo
│  └─────────────────────────────────────┘│
│                                         │
│  We'll scan your code for security      │
│  issues                                 │
└─────────────────────────────────────────┘
```

**State 5: Manual URL Input (폴백)**
```
┌─────────────────────────────────────────┐
│  GitHub Repository                      │
│                                         │
│  ┌─────────────────────────────────────┐│
│  │  https://github.com/user/repo      ││  ← URL 입력 필드
│  └─────────────────────────────────────┘│
│                                         │
│  Connect GitHub for private repos →     │  ← 텍스트 링크
│                                         │
│  We'll scan your code for security      │
│  issues                                 │
└─────────────────────────────────────────┘
```

#### GitHub OAuth API Endpoints

**`GET /api/auth/github`** — GitHub OAuth 인증 페이지 리다이렉트
- 응답: 302 redirect to `https://github.com/login/oauth/authorize?client_id=...&scope=repo,read:user`

**`GET /api/auth/github/callback`** — OAuth 콜백 처리 (code → access token 교환)
- 요청: `?code=xxx&state=xxx`
- 응답: 302 redirect to `/dashboard` (+ cookie/session 설정)

**`GET /api/github/repos`** — 연동된 계정의 repository 목록
- 요청: `?q=search_term` (선택)
- 응답:
```json
{
  "repos": [
    {
      "id": 123,
      "full_name": "my-org/private-app",
      "private": true,
      "default_branch": "main",
      "language": "TypeScript",
      "updated_at": "2026-02-20T10:00:00Z"
    }
  ]
}
```

**`GET /api/github/status`** — GitHub 연동 상태 확인
- 응답:
```json
{
  "connected": true,
  "username": "parkdev",
  "avatar_url": "https://avatars.githubusercontent.com/u/123"
}
```

**`DELETE /api/auth/github`** — GitHub 연동 해제
- 응답: `{ "success": true }`

#### GitHub 연동 Frontend 컴포넌트

```
new-project-dialog.tsx
├── GitHubConnectSection
│   ├── State: not_connected → "Connect GitHub" 버튼
│   ├── State: connecting → 로딩 스피너
│   ├── State: connected → username 표시 + repo 검색
│   └── State: manual → URL 입력 필드
└── RepoSearchDropdown
    ├── 검색 입력
    ├── Repo 목록 (🔒 private / 🌐 public 아이콘 구분)
    └── 선택된 repo 표시
```

### F3: Project Key

| 항목 | 내용 |
|------|------|
| 형식 | `gr_sk_` + 32자 (a-z, 0-9) |
| 발급 | 프로젝트 생성 시 자동 발급 |
| 표시 | 발급 직후 1회 전체 표시 + 복사 안내. 이후 마스킹 |
| 재발급 | 기존 키 즉시 무효화 + 새 키 발급 |
| 용도 | MCP 서버 인증, SDK 인증, API 호출 인증 |

### F4/F5: 보안 스캔 엔진

**MVP 스캔 항목 (Tier 1)**:

| # | 카테고리 | 체크 내용 | 사용자에게 보이는 말 |
|---|---------|----------|-------------------|
| 1 | 결제 | Stripe Secret Key 하드코딩 | Stripe 비밀 키가 안전한 곳에 있나요? |
| 2 | 결제 | Webhook 서명 검증 여부 | 결제 알림이 진짜인지 확인하나요? |
| 3 | 인증 | 비밀번호 평문 저장 여부 | 비밀번호가 암호화되어 저장되나요? |
| 4 | 인증 | 로그인 실패 횟수 제한 | 반복 로그인 시도를 막나요? |
| 5 | 비밀 정보 | .env 파일 Git 제외 여부 | 비밀 설정 파일이 GitHub에 올라가지 않나요? |
| 6 | 비밀 정보 | API 키 하드코딩 여부 | 모든 API 키가 안전한 곳에 보관되어 있나요? |
| 7 | 인프라 | HTTPS 적용 여부 | 데이터가 암호화되어 전달되나요? |
| 8 | 법적 | 개인정보처리방침 존재 | 개인정보 처리 안내 페이지가 있나요? |

**스캔 방식** (모두 로그인 필수):
- 웹 스캔: 프로젝트 설정에서 GitHub URL 입력 → 스캔 실행 → 결과 저장
- MCP 스캔: Claude Code에서 `scan_project` → 로컬 디렉토리 분석 → 결과 API 전송 + 저장

### F7: MCP 서버

**MCP가 제공하는 Tool 목록**:

| Tool 이름 | 설명 | 입력 | 출력 |
|----------|------|------|------|
| `authenticate` | Guardrail 계정 연결 (OAuth) | (없음 - 브라우저 팝업) | 인증 상태 + 사용자 정보 |
| `create_project` | 새 프로젝트 생성 | name, url (선택) | 프로젝트 정보 + project_key |
| `scan_project` | 현재 프로젝트 디렉토리 보안 스캔 | project_key (선택, 연결된 프로젝트 자동 감지) | 스캔 결과 (등급 + 항목별 결과) |
| `get_scan_result` | 마지막 스캔 결과 조회 | project_key (선택) | 최근 스캔 결과 |
| `get_fix_guide` | 특정 이슈의 수정 가이드 | issue_id | 수정 방법 + 코드 예시 |
| `list_projects` | 내 프로젝트 목록 | (없음) | 프로젝트 리스트 |
| `get_monitoring_status` | 모니터링 현황 조회 | project_key | Uptime, SSL, 최근 이벤트 |

**MCP 설정 방법** (Claude Code 기준):
```json
{
  "mcpServers": {
    "guardrail": {
      "command": "npx",
      "args": ["guardrail-mcp@latest"]
    }
  }
}
```
> Project Key가 필요 없음! 설치 후 `authenticate`로 계정 연결하면 끝.
> 프로젝트 생성/선택도 Claude Code 안에서 가능.

**사용자 경험 예시 — 최초 연결**:
```
사용자: "Guardrail 연결해줘"
Claude: Guardrail 계정을 연결할게요. 브라우저에서 로그인해주세요...
        → (OAuth 팝업 열림 → 로그인 완료)
        ✅ 연결 완료! park@email.com으로 로그인됐어요.

사용자: "내 앱 프로젝트 만들어줘"
Claude: Guardrail에 프로젝트를 생성할게요.
        ✅ "My App" 프로젝트 생성 완료!
        Project Key: gr_sk_a1b2c3... (자동 저장됨)
        바로 보안 스캔할까요?

사용자: "응 스캔해줘"
Claude: Guardrail로 보안 스캔을 실행할게요...

  🔍 스캔 완료! 보안 등급: C

  ✅ 통과 (6항목)
  - API 키 안전하게 보관됨
  - HTTPS 적용됨
  - ...

  🟡 고쳐야 할 것 (2항목)
  1. 비밀번호가 암호화되지 않았어요
     → bcrypt로 암호화하면 돼요. 고쳐드릴까요?
  2. 로그인 시도 제한이 없어요
     → rate limiter를 추가하면 돼요. 고쳐드릴까요?

사용자: "다 고쳐줘"
Claude: (수정 가이드를 기반으로 코드 수정 실행)
```

### F10: SDK 런타임 모니터링

**SDK 패키지**: `guardrail-sdk` (npm)

**미들웨어 구조** (Next.js / Express 호환):
```typescript
import { guardrail } from 'guardrail-sdk'

// Express
app.use(guardrail({ projectKey: process.env.GUARDRAIL_PROJECT_KEY }))

// Next.js middleware
export { guardrailMiddleware as middleware } from 'guardrail-sdk/next'
```

**SDK가 수집하는 이벤트**:
| 이벤트 | 수집 데이터 | 용도 |
|--------|-----------|------|
| `auth.login_failed` | IP, timestamp, user_agent | 브루트포스 감지 |
| `auth.login_success` | IP, geo, timestamp, user_id | 야간/이상 위치 감지 |
| `api.request` | endpoint, method, IP, timestamp | 트래픽 급증 감지 |

**SDK가 수집하지 않는 것** (프라이버시):
- 요청/응답 바디
- 사용자 비밀번호
- 쿠키/세션 내용
- 개인 식별 정보 (이름, 이메일 등)

### F11: 이메일 알림 시스템

| 항목 | 내용 |
|------|------|
| MVP 채널 | 이메일만 (Resend — API 키 설정 완료) |
| 알림 종류 | 즉시 알림만 (critical/warning 이벤트 발생 시) |
| 알림 설정 | 프로젝트별 `email_alerts_enabled` 토글 (기본 ON) |
| 쿨다운 | 동일 프로젝트 + 동일 알림 타입에 대해 1시간 내 중복 발송 금지 |

#### F11-1: 이메일 알림 트리거 조건

**Runtime Event 알림 (위험 이벤트 발생 시):**

| # | 이벤트 | 심각도 | 트리거 조건 | 이메일 제목 예시 |
|---|--------|--------|-----------|---------------|
| E1 | 브루트포스 감지 | Critical | 동일 IP 10분 내 로그인 실패 10회+ | "[프로젝트명] Brute force attack detected" |
| E2 | 트래픽 급증 | Warning | 평소 대비 요청량 500% 초과 | "[프로젝트명] Unusual traffic spike detected" |
| E3 | 야간 관리자 접근 | Warning | 새벽 1~5시 관리자 계정 로그인 | "[프로젝트명] Admin access at unusual hour" |

> **E4 (처음 보는 위치 로그인)은 이메일 대상에서 제외** — status가 "ok"(정보성)이므로 이메일 불필요. 대시보드에서만 표시.

**모니터링 알림 (이상 감지 시):**

| # | 감지 항목 | 심각도 | 트리거 조건 | 이메일 제목 예시 |
|---|----------|--------|-----------|---------------|
| M1 | 사이트 다운 | Critical | Uptime 체크 실패 (HTTP 응답 없음) | "[프로젝트명] Your site is down" |
| M2 | SSL 인증서 만료 임박 | Warning | 만료일 30일 이내 | "[프로젝트명] SSL certificate expires in N days" |
| M3 | SSL 인증서 무효 | Critical | 인증서 유효하지 않음 | "[프로젝트명] SSL certificate is invalid" |
| M4 | 보안 헤더 미흡 | Warning/Critical | 헤더 점수 80% 미만 | "[프로젝트명] Security headers need attention" |

> **status가 "ok"인 모니터링 결과는 이메일 대상에서 제외.** critical/warning만 발송.

#### F11-2: 이메일 내용 구조

```
┌─────────────────────────────────────────┐
│  🛡 Guardrail                           │
├─────────────────────────────────────────┤
│                                         │
│  [알림 유형 배지 + 심각도 색상]          │
│                                         │
│  [프로젝트명]                            │
│  [알림 제목]                             │
│                                         │
│  [상세 설명 — 무슨 일이 일어났는지]       │
│                                         │
│  상세 정보:                              │
│  IP: xxx.xxx.xxx.xxx                    │
│  시간: 10:23 AM                         │
│  ...                                    │
│                                         │
│  [대시보드에서 확인하기] ← 버튼           │
│                                         │
├─────────────────────────────────────────┤
│  이 알림은 프로젝트 설정에서 끌 수       │
│  있습니다.                              │
└─────────────────────────────────────────┘
```

| 요소 | 설명 |
|------|------|
| 브랜딩 | 상단 Guardrail 로고 + 다크 헤더 |
| 배지 색상 | Critical = 빨강 `#ed4242`, Warning = 주황 `#f59e0a` |
| 상세 정보 | 이벤트별 핵심 데이터 (IP, 시간, 위치, 수치 등) |
| CTA 버튼 | "View in Dashboard" → 해당 프로젝트 Overview로 이동 |
| 수신 거부 | 하단에 "프로젝트 설정에서 알림을 끌 수 있습니다" 안내 |

#### F11-3: 쿨다운 정책

| 규칙 | 설명 |
|------|------|
| 쿨다운 기간 | 1시간 |
| 쿨다운 단위 | 프로젝트 × 알림 타입 (예: Project A의 brute_force) |
| 구현 방식 | `monitoring_logs.email_sent_at` 컬럼으로 최근 발송 시간 확인 |
| 예시 | Project A에서 brute_force 이메일을 10:23에 보냄 → 11:23 전까지 동일 프로젝트의 brute_force 이메일 발송 안 함 |

#### F11-4: 알림 설정 UI

Project Settings에 Email Alerts 토글 추가:

```
┌────────────────────────────────────┐
│  General                           │
│  ┌──────────────────────────────┐  │
│  │ Project Name                 │  │
│  │ [My SaaS App          ]     │  │
│  │ Site URL                    │  │
│  │ [https://my-saas.app  ]     │  │
│  │                             │  │
│  │ Email Alerts          [ON]  │  │  ← 새로 추가
│  │ Get notified when we        │  │
│  │ detect security threats     │  │
│  │ or monitoring issues.       │  │
│  └──────────────────────────────┘  │
└────────────────────────────────────┘
```

| 요소 | 동작 |
|------|------|
| 토글 | ON/OFF — `projects.email_alerts_enabled` 업데이트 |
| 기본값 | ON (새 프로젝트 생성 시) |
| 범위 | 프로젝트별 독립 설정 |

---

## 4. 데이터 모델 (핵심 엔티티)

```
User
├── id (uuid)
├── email
├── name
├── avatar_url
├── provider (email / google / github)
├── github_access_token (encrypted, nullable — GitHub OAuth access token)
├── github_username (nullable — GitHub username)
├── github_avatar_url (nullable — GitHub profile avatar URL)
├── github_connected_at (datetime, nullable — GitHub 연동 시각)
├── created_at

Project
├── id (uuid)
├── user_id (FK → User)
├── name
├── url (nullable)
├── github_repo (nullable — OAuth 연동 시 "owner/repo" 형식, 수동 입력 시 full URL)
├── github_repo_id (number, nullable — GitHub repository ID)
├── github_repo_private (boolean — Private repo 여부)
├── project_key_hash (hashed)
├── project_key_prefix (gr_sk_a1b2, 표시용)
├── monitoring_enabled (boolean, default true)
├── sdk_connected (boolean, default false)
├── email_alerts_enabled (boolean, default true) ← Phase 3 F11
├── created_at

Scan
├── id (uuid)
├── project_id (FK → Project, nullable for anonymous)
├── source (web / mcp / auto)
├── target_url (GitHub URL 등)
├── grade (A~F)
├── total_checks
├── passed_checks
├── failed_checks
├── created_at

ScanItem
├── id (uuid)
├── scan_id (FK → Scan)
├── category (payment / auth / secrets / infra / legal)
├── check_key (stripe_key_exposed 등)
├── status (pass / fail / warn)
├── title_user (사용자에게 보이는 제목)
├── description_user (사용자에게 보이는 설명)
├── fix_guide (수정 가이드 마크다운)

MonitoringLog
├── id (uuid)
├── project_id (FK → Project)
├── type (uptime / ssl / header / runtime_event)
├── status (ok / warning / critical)
├── message (사용자에게 보이는 메시지)
├── metadata (jsonb — IP, geo, details 등)
├── email_sent_at (timestamptz, nullable — 이메일 발송 시간, 쿨다운 체크용) ← Phase 3 F11
├── created_at

RuntimeEvent (SDK에서 수신)
├── id (uuid)
├── project_id (FK → Project)
├── event_type (auth.login_failed / auth.login_success / api.request)
├── ip_address
├── geo_country
├── user_agent
├── metadata (jsonb)
├── created_at
```

---

## 4-1. Security Considerations (OAuth)

1. **Access Token 암호화**: GitHub access token은 DB에 AES-256으로 암호화하여 저장
2. **Token Scope 최소화**: 필요한 최소 scope만 요청 (`repo`, `read:user`)
3. **Token 갱신**: GitHub OAuth token은 만료되지 않지만, 사용자가 GitHub에서 revoke할 수 있으므로 401 응답 시 재인증 유도
4. **CSRF 방지**: OAuth state 파라미터로 CSRF 공격 방지
5. **Webhook Secret**: GitHub webhook 연동 시 secret 검증 필수

---

## 5. 개발 디렉터(Agent D) 기술 검토 의견

### Phase 1 (핵심 루프) — 동의
- F1~F6, F8은 하나의 흐름으로 연결됨. 여기까지 먼저 완성하고 검수받는 것이 맞다
- 예상 공수 8h. 빡빡하지만 가능

### Phase 2 (연결 & 모니터링) — 조건부 동의
- F7 MCP: Phase 1의 스캔 엔진을 그대로 재활용하므로 2h면 충분
- F9 URL 모니터링: cron + fetch로 단순 구현. 2h
- **F10 SDK: 가장 공수가 큰 항목. 현실적으로 3h는 최소치.**
  - npm 패키지 구조 + 미들웨어 + 이벤트 수집 API + 분석 로직
  - MVP에서는 이벤트 수집 + 저장까지만. 고급 분석은 이후
- F11 알림: Resend API 사용하면 1h

### 총 예상 공수
| Phase | 공수 | 누적 |
|-------|------|------|
| Phase 1 | ~8h | 8h |
| Phase 2 | ~8h | 16h |

### PM-Dev 조율 완료 사항
| 항목 | PM 의견 | Dev 의견 | 합의 |
|------|---------|---------|------|
| 비로그인 스캔 | 제거 (로그인 필수로 변경) | 동의 — 전환율/데이터 품질 향상 | **로그인 필수** |
| GitHub 방식 | OAuth 연동으로 private 포함 | Private repo OAuth 필요 | **OAuth 연동 (private 포함) + 수동 URL 폴백 (public only)** |
| Key 마스킹 | 발급 후 마스킹 | hash 저장 시 원본 복구 불가 | **발급 시 1회 표시 + hash + prefix 저장** |
| 스캔 시간 | "60초" 마케팅 | 실제 30초 미만 가능 | **"보통 30초 내" + 2분 타임아웃** |
| SDK 마찰 | 마찰 아님 (바이브코더는 AI가 설치) | 동의 | **MVP 포함 확정** |

---

## 6. 화면 목록 & 라우팅 총정리

| # | 화면 | 경로 | 인증 | Phase |
|---|------|------|------|-------|
| 1 | Landing Page (Hero + Features + Pricing + Footer) | `/` | No | 1 |
| 1a | └ Features 섹션 | `/#features` | No | 1 |
| 1b | └ Pricing 섹션 | `/#pricing` | No | 1 |
| L1 | Login (이메일+비밀번호, Google/GitHub OAuth) | `/login` | No | 1 |
| L2 | Sign Up (이름, 이메일, 비밀번호, Google/GitHub OAuth) | `/signup` | No | 1 |
| L3 | Email Verification Pending (이메일 인증 대기) | `/auth/verify-email` | No | 2 |
| L4 | Forgot Password (비밀번호 재설정 이메일 요청) | `/auth/forgot-password` | No | 2 |
| L5 | Reset Password (새 비밀번호 설정) | `/auth/reset-password` | No | 2 |
| 2 | Overview (등급 + 모니터링 + SDK이벤트 + 최근스캔 + 등급 가이드) | `/dashboard` | Yes | 1 |
| 3 | Scan Result (스캔 상세) | `/project/[id]/scan/[scanId]` | Yes | 1 |
| 4 | Project Settings (General, MCP, Danger Zone) | `/project/[id]/settings` | Yes | 1 |
| 5 | Scan History (스캔 히스토리 전체 목록) | `/project/[id]/scans` | Yes | 1 |
| 6 | Profile Settings (프로필, Appearance) | `/settings/profile` | Yes | 1 |
| 7 | Plan & Billing (현재 플랜, 업그레이드) | `/settings/billing` | Yes | 1 |
| E1 | Empty State — No Projects (첫 로그인) | `/dashboard` | Yes | 1 |
| E2 | Empty State — No Scans (프로젝트 생성 후) | `/dashboard` | Yes | 1 |
| T1 | Terms of Service (이용약관) | `/terms` | No | 2 |
| T2 | Privacy Policy (개인정보처리방침) | `/privacy` | No | 2 |

**사이드바 네비게이션 (4개):**
- 📊 Overview — 프로젝트 전체 현황판 (등급 + 모니터링 + SDK이벤트 + 최근스캔)
- 🔍 Scan — 보안 스캔 실행 + 결과
- 📋 Scan History — 스캔 히스토리 전체 목록
- ⚙️ Settings — 프로젝트 설정

---

## 7. 빌드 순서 & 검수 포인트

```
Phase 1: 핵심 루프
├── F1 Auth (이메일+비밀번호, Google, GitHub OAuth)
├── F2 프로젝트 CRUD (최대 3개) + 프로젝트 드롭다운
├── F3 Project Key 발급
├── F4 스캔 엔진 (Tier 1, 8항목)
├── F5 로그인 스캔 (히스토리 저장)
├── F6 결과 화면 (등급 + 카드)
├── F8 대시보드 (Overview + Scans History)
├── Empty States (No Projects, No Scans)
└── 🔍 PM 디렉터 검수 → 통과 시 Phase 2 진행

Phase 2: 백엔드 + 실제 동작 + 연결 & 모니터링
├── 인프라: Supabase (DB + Auth) + Stripe + Vercel 배포
├── F1~F6, F8 백엔드 실제 동작 (Auth/CRUD/스캔엔진/Mock교체)
├── F7 MCP 서버 (7 Tools, npm 패키지)
├── F9 URL 모니터링 (Vercel Cron — Uptime/SSL/Headers/재스캔)
├── F10 SDK 런타임 모니터링 (npm 패키지 + 이벤트 수집)
├── 결제: Stripe 연동 (Checkout/Portal/Webhook)
├── 법적: 이용약관 + 개인정보처리방침
└── 🔍 PM 디렉터 최종 검수

Phase 3: 이메일 알림 (Phase 2 완료 후)
├── F11 이메일 알림 (Resend)
├── Resend 패키지 설치 + 이메일 유틸리티 생성
├── DB 마이그레이션 (email_alerts_enabled, email_sent_at)
├── Runtime Event 알림 (brute force, traffic spike, admin access)
├── 모니터링 알림 (사이트 다운, SSL 만료, 보안 헤더 미흡)
├── 쿨다운 로직 (1시간 중복 방지)
├── Project Settings에 Email Alerts 토글 추가
└── 🔍 PM 디렉터 검수
```

**PM 디렉터 코멘트:**
Phase 1이 끝나면 알려주세요. 핵심 루프(가입 → 프로젝트 생성 → 스캔 → 결과 확인)가 매끄럽게 돌아가는지 검수합니다. Phase 1이 안 되면 Phase 2는 의미 없습니다.

> **v1.3 변경 (2026-02-23)**: Phase 2 착수 전 PM Director 정합성 리뷰. 주요 변경사항:
> 1. F11 알림 시스템을 Phase 2 → Phase 3으로 이관 (Implementation Plan과 정합성 확보)
> 2. Phase 2 범위를 "백엔드 + 실제 동작 + 연결 & 모니터링"으로 확장 (12-Step 구현 계획과 일치)
> 3. Screen L3/L4/L5 추가 (Email Verification, Forgot Password, Reset Password 화면 정의)
> 4. Screen 5 Monitoring Dashboard → Overview 통합 결정 확정
> 5. Screen 3-1 Scan History 와이어프레임 추가
> 6. 라우팅 테이블에 L3~L5, T1~T2 (법적 문서) 추가

> **MCP Phase 1 당기기 권고 검토 (Agent C 결정서 참조):**
> Agent C가 MCP를 Phase 1으로 당기는 것을 권고했으나, 아래 이유로 Phase 2 유지:
> - Phase 1은 "프론트엔드 목 데이터 기반 UI 완성"으로 범위를 한정. MCP는 백엔드 스캔 엔진(Step 4) 완성 후에야 의미 있음.
> - Phase 2 Step 5에서 스캔 엔진을 그대로 재활용하여 MCP를 구현하므로, 실질적으로 Phase 2 초중반에 빠르게 도달.
> - Phase 1 UI가 완성되지 않은 상태에서 MCP를 먼저 구현하면, 스캔 결과 표시 화면 없이 MCP만 동작하는 불완전한 상태가 됨.
> - **결론**: Phase 2 내에서 Step 5 우선순위를 높이는 것으로 타협. MCP는 스캔 엔진(Step 4) 직후 바로 구현.
